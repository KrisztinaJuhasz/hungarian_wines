import { FunctionComponent } from "react";

const Carousel: FunctionComponent<{}> = (): JSX.Element => {
    return (
        <div className='carousel-container'>
            <p>This is the placeholder for the carousel.</p>
        </div>
    );
}

export default Carousel;
